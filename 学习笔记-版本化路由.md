# 学习笔记：版本化路由（API Versioning）

> [返回项目学习笔记](FastApi项目学习笔记.md)｜[返回 Windows 10 入门手册](FastAPI项目入门手册-Windows10.md)

## 一句话理解

版本化路由就是：**在后端 API 地址中加上版本号，让新旧接口可以同时存在。**

例如：

```text
GET /api/v1/users
GET /api/v2/users
```

这里的 `v1` 表示第一版接口，`v2` 表示第二版接口。

## 它和常见路由有什么区别？

| 概念 | 作用 | 示例 |
| --- | --- | --- |
| Vue 前端路由 | URL 对应一个页面或组件 | `/user/1` 对应用户详情页 |
| 网关路由 | 根据路径把请求转发到某个服务 | `/order/**` 转到订单服务 |
| 后端 API 路由 | URL 对应一个 Controller/接口处理函数 | `/api/v1/users` 对应查询用户 |
| 版本化路由 | 给 API 路由加上版本号，区分不同代接口 | `/api/v1/users` 与 `/api/v2/users` |

它不是新的底层技术，仍然是“URL 匹配到后端处理函数”。只是团队在 URL 中增加版本号的约定。

## 为什么需要版本号？

接口上线后，网页前端、手机 App 或第三方系统可能都在调用它。假如旧接口原来返回：

```json
{ "name": "张三" }
```

后来业务需要更多数据，想改成：

```json
{ "id": 1, "nickname": "张三", "avatar": "..." }
```

若直接修改旧地址，正在使用旧格式的客户端可能报错。更安全的做法是：

- `/api/v1/users`：保持旧返回格式，兼容旧客户端；
- `/api/v2/users`：使用新返回格式，供新客户端接入。

等所有调用方升级后，再按团队约定逐步废弃 `v1`。

## 对应到 Spring Boot 和 FastAPI

Spring Boot 中可以这样写：

```java
@RequestMapping("/api/v1/users")
public class UserController {
}
```

FastAPI 常见写法是统一给一个路由组加前缀：

```python
app.include_router(router, prefix="/api/v1")
```

这个项目中，`main.py` 会挂载 `routes/api.py` 的路由；版本前缀通常就在这类路由入口统一配置。之后再由路由转给各个 Controller。

## 本项目 URL 拼接全过程

以登录接口为例，最终 URL 是 `/api/v1/login/`，它由三段前缀"层层叠加"而成：

```text
/api/v1  +  /login  +  /
────────    ──────    ─
  第一层      第二层    第三层
```

### 第一层：main.py → /api/v1

```python
# main.py
fullAPIPrefix = str("/%s/%s" % (settings.apiPrefix, settings.apiVersion))
fastApiApp.include_router(router, prefix=fullAPIPrefix)
```

`settings.apiPrefix` 的值是 `"api"`，`settings.apiVersion` 的值是 `"v1"`，拼接后得到 **`/api/v1`**。

这一层是**全局统一前缀**，所有业务路由都会被挂在这个前缀下面，这样以后升级版本（v1 → v2）只需改一处配置。

### 第二层：routes/api.py → 无额外前缀（纯中转）

```python
# routes/api.py
router = APIRouter()

def includeApiRoutes():
    router.include_router(login_api)  # 不加 prefix，原样透传
```

这个文件是一个**路由聚合点**，它本身没有加任何 `prefix`，只是把各个 Controller 的 Router 汇总起来，再交给 `main.py`。所以这一层对 URL **没有贡献**。

### 第三层：login.py → /login + /

```python
# app/api/controllers/auth/login.py
router = APIRouter(
    prefix='/login'
)

@router.post("/", response_model=dict)
async def loginUser(body: LoginSchema):
    ...
```

- `APIRouter(prefix='/login')` 给这个 Controller 加上了业务前缀 **`/login`**
- `@router.post("/")` 表示路由路径是 **`/`**（即该前缀下的根路径）

两者拼接得到：**`/login/`**

### 最终拼接一览

| 层级 | 来源文件 | 贡献的路径段 |
|------|---------|-------------|
| 全局前缀 | `main.py` | `/api/v1` |
| 聚合路由 | `routes/api.py` | _(无额外前缀)_ |
| Controller 前缀 | `login.py` | `/login` |
| 路由路径 | `@router.post("/")` | `/` |

```text
/api/v1  +  /login  +  /  =  /api/v1/login/
```

### 为什么要这样分层？

- **Controller 只管业务**：`login.py` 只需知道自己处理 `/login` 相关请求，不需要关心版本号。
- **版本升级集中管理**：如果将来要出 v2，只需改 `settings.py` 里的 `apiVersion`，所有 Controller 的路径自动变成 `/api/v2/...`，不用逐个修改。
- **路由聚合清晰**：`routes/api.py` 作为统一入口，一眼就能看出挂载了哪些业务模块。

## 小结

版本化路由解决的核心问题是：**后端可以迭代接口，同时尽量不让旧客户端因为接口变更而失效。**
